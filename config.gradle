// 整个项目的配置
ext {
    app = [
            compileSdkVersion: 30,
            minSdkVersion    : 16,
            targetSdkVersion : 30,
            versionCode      : 1,
            versionName      : "1.0"
    ]

    // 公共配置
    versions = [
            // 阿里的ARouter
            appcompat: 'androidx.appcompat:appcompat:1.1.0',
            base     : "3.5.0",
    ]
}